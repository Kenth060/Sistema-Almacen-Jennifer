<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registro</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"rel="stylesheet"integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"crossorigin="anonymous"/>
    <link href="https://unpkg.com/boxicons@2.0.7/css/boxicons.min.css"rel="stylesheet"/>
    <link rel="stylesheet" href="/resources/css/style.css" />
    
    <link rel="shortcut icon" href="/resources/images/favicon.ico">
</head>
<body>
    <div class="register-form">
		<h1>Almacen Comercial Jennifer</h1>
        <h1>Registro de Usuario</h1>
        <form action="/register" method="POST">
        
            <label for="ColabName" class="fw-bold">Colaborador del Almacen</label>
            <select class="form-select my-2"  aria-label="Miembro del Almacen" id="ColabName" name="ColabName"  required>
                <option disabled value="" >Seleccione un colaborador del Almacen</option>
                <% Colaboradores.forEach((C) => { %>
                    <option value="<%= C.Id_Persona %>" selected><%= C.Nombre %></option>
                <% }) %>
            </select>
            
            <label for="user" class="fw-bold mt-1">Nombre de Usuario</label>
            <input type="text" name="ColabUser" class="form-control my-1" placeholder="Usuario"  value="<%= username %>" required>

            <label for="rol" class="fw-bold mt-1">Rol del Colaborador</label>
            <select name="ColabRol" id="ColabRol" class="form-select my-1">
                <% if( Tipo != 'Vendedor') { %>
                    <option value="Gerente" selected><%= Tipo %></option>
                <% } else{ %>
                    <option value="Vendedor" selected><%= Tipo %></option>
                <% } %>
                
                
            </select>

            <label for="pass" class="fw-bold mt-1">Contraseña</label>
            <input type="password" name="ColabPass" class="form-control my-1" required>

            <div style="display: inline;" class="d-flex align-items-center justify-content-center">
                <a href="/" class="btn btn-danger mx-2">Cancelar</a>
                <input type="submit" class="btn btn-success mx-2" value="Guardar">
            </div>

		</form>		
	</div>

    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <% if (typeof alert != "undefined")  { %>
        <script>
            Swal.fire({
                title: '<%= alertTitle %>',
                text: '<%= alertMessage %>',
                icon: '<%= alertIcon %>',
                showConfirmButton: <%= showConfirmButton %> ,
                timer: <%= timer %>
            }).then(() => {
                window.location = '/ <%= ruta %>'
            })
        </script>
    <% } %>
    
</body>
</html>